<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Azure WebApp — Charts from JSON</title>
  <style>
    body {
      font-family: Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
      margin: 18px;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
    }

    h1 {
      font-size: 1.4rem;
      margin-bottom: 6px;
    }

    p {
      margin-top: 0;
      color: #444;
    }

    .chart-wrap {
      display: grid;
      grid-template-columns: 1fr;
      gap: 18px;
    }

    .card {
      padding: 12px;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
      background: #fff;
    }

    canvas {
      width: 100% !important;
      height: 360px !important;
    }

    .meta {
      margin-top: 8px;
      font-size: 0.9rem;
      color: #666;
    }

    .error {
      color: #b00020;
    }
  </style>
</head>

<body>

  <div class="container">
    <h1>Mockup charts (last 72h)</h1>

    <div class="chart-wrap">

      <pre id="rawJson" class="card" style="white-space: pre-wrap; max-height: 300px; overflow:auto;"></pre>
      <div class="meta">Temporary JSONs logger.</div>

      <div id="log" class="meta"></div>
    </div>
  </div>

  <!-- Chart.js (views) -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <!-- Client bootstrap (module) — served from /public -->
  <script type="module" src="/client-app.js"></script>
  <!-- chart-logic.js will be imported by client-app.js via relative import './chart-logic.js' -->

</body>

</html>